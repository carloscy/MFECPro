<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ec.marcafut.marcafut0011">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/marcafutlogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ResultadosActivity"></activity>
        <activity android:name=".TablagoleadoresActivity" />
        <activity android:name=".Acceso.ResetPasswordActivity" />
        <activity android:name=".Acceso.ProfileActivity" />
        <activity android:name=".Acceso.LoginActivity" />
        <activity android:name=".Acceso.RegistroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Noticias.SeriebActivity" />
        <activity android:name=".Noticias.CopaecActivity" />
        <activity android:name=".Noticias.SuperLigaActivity" />
        <activity android:name=".Noticias.FefActivity" />
        <activity android:name=".Tabla" />
        <activity android:name=".NoticiasActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>